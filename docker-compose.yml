services:
  server:
    build: ./server
    container_name: gitcompass-server
    ports:
      - "8000:8000"
    env_file:
      - ./server/.env
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: gitcompass-redis
    ports:
      - "6379:6379"
